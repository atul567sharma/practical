define([
    'uiComponent'

], function (Component) {
    'use strict';

    return Component.extend({
        defaults: {
            template: 'Seepossible_Checkout/checkout/shipping/additional-block'
        }
    });
});
